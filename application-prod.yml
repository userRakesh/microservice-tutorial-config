 spring:
  application:
    name: CONFIG-PROD


eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8080/eureka/
    registerWithEureka: true
    fetchRegistry: true
    instance:
      preferIpAddress: true
